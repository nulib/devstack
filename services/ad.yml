volumes:
  data:
service_definition:
  image: nulib/ldap-alpine
  volumes:
    - type: volume
      source: <%= volume('data') %>
      target: /var/lib/samba
  ports:
    - <%= offset_port(389) %>:389
    - <%= offset_port(636) %>:636
  environment:
    DOMAIN: library.northwestern.edu
    DOMAINPASS: d0ck3rAdm1n!
